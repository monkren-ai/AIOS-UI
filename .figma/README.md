# Figma Export Reference

This directory contains auto-generated code exported from Figma. These files are **reference-only** and should NOT be used as production code.

## Status

- Code uses `autoWrapper` patterns and raw `ellipse` elements
- No design system token references (hardcoded values throughout)
- Not aligned with the Nothing UI Kit component library

## Usage

- Layout reference only — use as a visual guide for component structure
- For production implementations, use the components in `web-ui-kit/react/src/components/` or `web-ui-kit/css/`
- All styling should reference `tokens.css` design tokens
